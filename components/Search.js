import React from "react";
import {
  View,
  Text,
  StyleSheet,
  Button,
  TextInput,
  TouchableOpacity
} from "react-native";

const Search = () => {
};

const styles = StyleSheet.create({
  block: {
    height: 40,
    paddingHorizontal: 16,
    borderColor: "#bdbdbd",
    borderTopWidth: 1,
    borderBottomWidth: 1,
    alignItems: "center",
    flexDirection: "row"
  },
  input: {
    flex: 1
  },
  button: {
    marginLeft: 8
  }
});

export default Search;
